import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DLM - Da Logistics Manager | Smart Logistics Platform",
  description: "DLM is a comprehensive logistics management platform for fleet tracking, ticket management, and real-time delivery monitoring. Streamline your logistics operations with our cutting-edge technology.",
  keywords: "logistics, fleet management, delivery tracking, ticket management, transportation, supply chain",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
